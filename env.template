# Spotify API credentials
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
# SPOTIFY_USERNAME is the username to fetch all public playlists for that user
SPOTIFY_USERNAME=
# SPOTIFY_PLAYLIST_ID is optional - comma-separated list of playlist IDs (can also be provided via command line)
SPOTIFY_PLAYLIST_ID=
# SPOTIFY_PLAYLIST_EXCLUDED_ID is optional - comma-separated list of playlist IDs to exclude from processing
SPOTIFY_PLAYLIST_EXCLUDED_ID=

# Plex Configuration
PLEX_URL=http://your_plex_server:32400
PLEX_TOKEN=your_plex_token_here
PLEX_LIBRARY_SECTION_ID=your_music_library_section_id
# PLEX_SERVER_ID is the unique identifier for your Plex server (can be auto-discovered if not set)
# Leave empty to auto-discover from the server, or set manually if auto-discovery fails
PLEX_SERVER_ID=

# Playlist Configuration
# Note: You can set SPOTIFY_USERNAME, SPOTIFY_PLAYLIST_ID, or both.
# If both are set, playlists will be combined and deduplicated.
# At least one of SPOTIFY_USERNAME or SPOTIFY_PLAYLIST_ID must be set.